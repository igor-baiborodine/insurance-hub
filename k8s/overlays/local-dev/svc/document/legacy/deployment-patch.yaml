apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-api-legacy
spec:
  template:
    spec:
      containers:
        - name: document-api-legacy
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 300m
              memory: 512Mi
          env:
            # Postgres sensitive data
            - name: PG_USER
              valueFrom:
                secretKeyRef:
                  name: local-dev-postgres-document-user-creds
                  key: username
            - name: PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: local-dev-postgres-document-user-creds
                  key: password
            # MinIO sensitive data
            - name: MINIO_SVC_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: local-dev-minio-document-svc-user-creds
                  key: MINIO_SVC_ACCESS_KEY
            - name: MINIO_SVC_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: local-dev-minio-document-svc-user-creds
                  key: MINIO_SVC_SECRET_KEY
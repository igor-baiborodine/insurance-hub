apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: local-dev-all
namePrefix: local-dev-
labels:
  - includeSelectors: true
    pairs:
      environment: local-dev
resources:
  - ../../../../../apps/svc/document/base/legacy
patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: document-api-legacy
configMapGenerator:
  - name: document-config-legacy
    behavior: merge
    literals:
      - MINIO_TENANT_ENDPOINT=http://local-dev-minio-document-hl.local-dev-minio-document.svc.cluster.local:9000
      - PG_HOST=local-dev-postgres-document-rw.local-dev-all.svc.cluster.local
      - KAFKA_HOST=local-dev-kafka-kafka-bootstrap.local-dev-all.svc.cluster.local
      - TRACING_ZIPKIN_ENABLED=false
      - JSREPORT_HOST=local-dev-jsreport.local-dev-all.svc.cluster.local
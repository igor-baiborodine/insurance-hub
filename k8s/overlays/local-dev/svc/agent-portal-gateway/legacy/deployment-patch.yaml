apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-portal-gateway-legacy
  labels:
    environment: local-dev
spec:
  template:
    metadata:
      labels:
        environment: local-dev
    spec:
      containers:
        - name: agent-portal-gateway-legacy
          env:
            - name: MICRONAUT_ENVIRONMENTS
              value: "local-dev"
            - name: TRACING_ZIPKIN_ENABLED
              value: "false"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 300m
              memory: 512Mi

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: local-dev-all
namePrefix: local-dev-
labels:
  - includeSelectors: true
    pairs:
      environment: local-dev
resources:
  - ../../../../../apps/svc/pricing/base/legacy
patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: pricing-api-legacy
configMapGenerator:
  - name: pricing-config-legacy
    behavior: merge
    literals:
      - PG_HOST=local-dev-postgres-pricing-rw.local-dev-all.svc.cluster.local
      - TRACING_ZIPKIN_ENABLED=false

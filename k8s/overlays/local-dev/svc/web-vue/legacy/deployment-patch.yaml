apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-vue-legacy
spec:
  template:
    spec:
      containers:
        - name: web-vue-legacy
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1Gi
              cpu: 500m
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: qa-svc
namePrefix: qa-
labels:
  - includeSelectors: true
    pairs:
      environment: qa
resources:
  - ../../../../../apps/svc/pricing/base/legacy
patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: pricing-api-legacy
configMapGenerator:
  - name: pricing-config-legacy
    behavior: merge
    literals:
      - PG_HOST=qa-postgres-pricing-rw.qa-data.svc.cluster.local
      - TRACING_ZIPKIN_ENABLED=true
      - ZIPKIN_HOST=qa-zipkin.qa-monitoring.svc.cluster.local
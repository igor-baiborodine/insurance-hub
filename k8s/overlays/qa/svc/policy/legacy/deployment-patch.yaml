apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-api-legacy
spec:
  template:
    spec:
      containers:
        - name: policy-api-legacy
          image: ghcr.io/igor-baiborodine/insurance-hub/policy-api-legacy:1.0.0
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 100m
              memory: 384Mi
            limits:
              cpu: 300m
              memory: 768Mi
          env:
            - name: MICRONAUT_ENVIRONMENTS
              value: "qa"
            # Postgres sensitive data
            - name: PG_USER
              valueFrom:
                secretKeyRef:
                  name: qa-postgres-policy-user-creds
                  key: username
            - name: PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: qa-postgres-policy-user-creds
                  key: password
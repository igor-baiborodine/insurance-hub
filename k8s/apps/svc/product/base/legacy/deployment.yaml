apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-api-legacy
  labels:
    app.kubernetes.io/name: product-api-legacy
    app.kubernetes.io/instance: insurance-hub-product-api-legacy
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: product-api-legacy
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: product-api-legacy
        app.kubernetes.io/instance: insurance-hub-product-api-legacy
        app.kubernetes.io/version: "latest"
        app.kubernetes.io/component: api
    spec:
      containers:
        - name: product-api-legacy
          envFrom:
            - configMapRef:
                name: product-config-legacy
          image: insurance-hub-product-api-legacy:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: KUBERNETES_CLIENT_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: MICRONAUT_SERVER_HOST
              value: "0.0.0.0"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
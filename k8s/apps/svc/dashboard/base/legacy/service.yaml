apiVersion: v1
kind: Service
metadata:
  name: dashboard-api-legacy
  labels:
    app.kubernetes.io/name: dashboard-api-legacy
    app.kubernetes.io/instance: insurance-hub-dashboard-api-legacy
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dashboard-api-legacy
    app.kubernetes.io/component: api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
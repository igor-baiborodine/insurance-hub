apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minio-document-service-monitor
  namespace: qa-minio-document
  labels:
    release: qa-prometheus
spec:
  selector:
    matchLabels:
      app: qa-minio-document
  namespaceSelector:
    matchNames:
      - qa-minio-document
  endpoints:
    - port: https-minio
      path: /minio/v2/metrics/cluster
      interval: 30s
